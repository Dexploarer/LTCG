/**
 * Top-level seedStarterCards export for backward compatibility
 * Maintains flat API structure (api.seedStarterCards.*) while code is organized in subdirectories
 */

export { seedStarterCards } from "./scripts/seedStarterCards";
